# Framework adapters

Reserved for future React / Preact / Solid / Astro wrappers. Adapters
translate the core's `EventEmitter` into framework-idiomatic primitives
(hooks, signals, effects) without business logic.

See the parent `README.md` §"Framework adapters" for the intended shape
of each adapter.
